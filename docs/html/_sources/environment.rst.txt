=====================
Environment Variables
=====================


CONSTRUCT_ROOT
    Root directory containing your projects. Defaults to `~/projects`.

CONSTRUCT_CONFIG
    Path to your Construct config file.
    If not set Construct will use `construct/defaults/construct.yaml`.
